[text](inpaint_xl_ref.json) : inpaint_sdxl.json应该参考这个生成，

[text](inpaint_k_ref.json)：inpaint_klein.json应该参考这个，有一个被跳过的lora节点，也需要做一个开关让用户选择开启，可以参考inpaintflux里的可选lora

[text](upscale_ref.json)：upscale.json 应该参考这个，但是需要把提示词换成传入图反推